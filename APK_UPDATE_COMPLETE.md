# 📱 دليل تحديث APK - النسخة 1.5.0

## ✅ ما تم تحديثه:

```
✅ version في package.json: 0.1.0 → 1.5.0
✅ versionCode في build.gradle: 1 → 3
✅ versionName في build.gradle: 1.0 → 1.5.0
```

---

## 🚀 خطوات البناء (اختر طريقة واحدة)

### الطريقة 1️⃣: Android Studio (الأسهل والأسرع)

#### الخطوة 1: التحضير
```bash
# من جذر المشروع:
npm run build          # بناء موارد الويب
npx cap copy android   # نسخ الموارد إلى Android
npx cap open android   # فتح Android Studio تلقائياً
```

#### الخطوة 2: البناء
```
في Android Studio:
1. انتظر انتهاء الفهرسة (Indexing)
2. اذهب إلى: Build → Build Bundle(s)/APK(s) → Build APK(s)
3. حدد: Release (لـ النشر) أو Debug (للاختبار)
4. اضغط OK
5. سيبدأ البناء (استغرق 2-5 دقائق)
6. عند الانتهاء، اضغط "Locate" لفتح مجلد APK
```

**النتيجة:**
```
📁 android/app/build/outputs/apk/release/
   └── app-release.apk ✅
```

---

### الطريقة 2️⃣: Command Line (عبر Gradle)

```bash
# الانتقال إلى مجلد Android
cd android

# بناء APK للاختبار (أسرع):
./gradlew assembleDebug
# النتيجة: app-debug.apk

# بناء APK للنشر (مُنتج):
./gradlew assembleRelease
# النتيجة: app-release.apk
```

#### في Windows:
```bash
cd android
gradlew.bat assembleRelease
```

---

### الطريقة 3️⃣: عبر Capacitor (متقدمة)

```bash
# من جذر المشروع
npx cap build android --keystorepath ./key.jks --keystorealiasname duaii-key

# أو استخدم Android Studio بعد
npx cap open android
```

---

## 📲 تثبيت APK على الجهاز

### الطريقة 1: عبر Android Studio
```
1. في Android Studio
2. Run → Select Device
3. اختر جهازك من القائمة
4. اضغط Run

# أو مباشرة:
Run → Run 'app'
```

### الطريقة 2: عبر ADB
```bash
# التأكد من توصيل الجهاز:
adb devices

# تثبيت APK:
adb install android/app/build/outputs/apk/release/app-release.apk

# أو (إذا كان مثبتاً بالفعل):
adb install -r android/app/build/outputs/apk/release/app-release.apk
```

### الطريقة 3: نقل يدوي
```bash
# انسخ الملف:
# android/app/build/outputs/apk/release/app-release.apk

# انقله إلى هاتفك عبر:
# - كابل USB وتطبيق مدير الملفات
# - تطبيق نقل الملفات
# - البريد الإلكتروني
# - التطبيق التخزين السحابي

# ثم افتح الملف على الجهاز
# التطبيق سيتم تثبيته تلقائياً
```

---

## 🧪 اختبار التطبيق

بعد التثبيت، اختبر:

✅ **تسجيل الدخول:**
- فتح التطبيق
- تسجيل دخول بحساب جديد
- قبول الأذونات

✅ **الموقع الجغرافي:**
- فتح خريطة الصيدليات
- تفعيل الموقع
- عرض الصيدليات القريبة

✅ **الإشعارات:**
- استقبال إشعارات جديدة
- النقر على الإشعار

✅ **الوصفات:**
- رفع وصفة جديدة
- عرض الصيدليات القريبة
- اختيار صيدلية

✅ **الأداء:**
- سرعة التحميل
- عدم التأخر
- استهلاك البطارية

---

## 🐛 استكشاف الأخطاء الشائعة

### خطأ: "Failed to build"
```bash
# الحل:
cd android
./gradlew clean
./gradlew build
```

### خطأ: "SDK not found"
```bash
# تأكد من تثبيت Android SDK في:
# Windows: C:\Users\<username>\AppData\Local\Android\Sdk
# Mac: ~/Library/Android/sdk
# Linux: ~/Android/Sdk
```

### خطأ: "Connection timeout"
```bash
# الحل:
1. تحقق من اتصال الإنترنت
2. جرب gradle offline mode
```

### APK كبير الحجم جداً؟
```bash
# الحل:
# استخدم Release build (أصغر حجماً)
# لا تستخدم Debug build للنشر
```

---

## 📊 معلومات APK الجديد

| المعلومة | القيمة |
|---------|--------|
| **اسم التطبيق** | duaii |
| **معرّف التطبيق** | com.duaiii.app |
| **الإصدار** | 1.5.0 |
| **رمز الإصدار** | 3 |
| **حجم التطبيق** | ~50-100 MB |
| **الحد الأدنى SDK** | 24 (Android 7.0+) |
| **الحد الأقصى SDK** | 34 (Android 14) |

---

## 🔒 الأمان والتوقيع

### مهم جداً:
```bash
# تم توقيع Release APK تلقائياً بمفتاح عام
# إذا أردت مفتاح توقيع خاص:

keytool -genkey -v -keystore duaii-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias duaii-key

# احفظ الملف في مكان آمن!
# لا تفقده أبداً!
```

---

## 📤 نشر على Google Play

بعد اختبار APK بنجاح:

1. **إنشاء حساب مطور:**
   - اذهب إلى: https://play.google.com/console
   - ادفع رسم التسجيل ($25)

2. **إنشاء التطبيق:**
   - Add New App
   - اسم التطبيق: duaii
   - الفئة: Health & Fitness

3. **إضافة البيانات:**
   - وصف التطبيق
   - الكلمات الرئيسية
   - الصور (4-8 صور)
   - الفيديو الترويجي (اختياري)

4. **تحميل APK:**
   - Upload → app-release.apk
   - ملء البيانات الإضافية
   - الموافقة على السياسات

5. **الانتظار:**
   - المراجعة: 2-4 ساعات عادة
   - النشر: تلقائي بعد الموافقة

---

## ✨ نصائح مهمة

✅ **قبل البناء:**
- تأكد من عمل جميع الميزات محلياً
- اختبر على إصدارات مختلفة من Android
- تأكد من الإذونات

✅ **أثناء البناء:**
- لا تغلق Android Studio
- احفظ المفتاح الخاص بالتوقيع
- احتفظ بنسخة احتياطية من APK

✅ **بعد البناء:**
- اختبر على جهاز فعلي
- تحقق من الأداء والبطارية
- توثيق رقم الإصدار

---

## 📞 المساعدة الإضافية

للمزيد من المعلومات:
- [Capacitor Docs](https://capacitorjs.com/docs/android)
- [Android Developer](https://developer.android.com)
- [Google Play Console Help](https://support.google.com/googleplay/android-developer)

---

**تم التحضير! 🎉**

اتبع الخطوات أعلاه وستتمكن من:
✅ بناء APK جديد بنجاح
✅ تثبيته على جهازك
✅ نشره على Google Play

**حظاً موفقاً! 🚀**
